# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# Pytest
.pytest_cache/
.coverage
htmlcov/

# Project data (cache + DB are gitignored; HTML fixtures under tests/ are NOT)
data/cache/
data/*.duckdb
data/*.duckdb.wal

# PFR browser session config (contains cf_clearance cookie — never commit)
data/pfr_session.json

# Editors / tooling metadata
.vscode/
.idea/
.claude/
*.swp
.DS_Store
